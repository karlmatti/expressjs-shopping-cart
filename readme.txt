How to run the api with custom port
1. Install dependencies with `npm install`

2. Define port and run
    a. PRODUCTION
        windows example: SET PORT=3002 && node index.js
    b. DEV
        windows example: SET PORT=3002 && nodemon index.js

Unit tests for calculations with `npm test`